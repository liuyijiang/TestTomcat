###########################################################
#   tomcat study                                          #
###########################################################

###############################
part one     tomcat组成   
1连接器
2处理器
################################
ext1 第一个基于socket 的request response 服务器
ext2 第二个应用程序由6个类组成：
ext3 Catalina中有两个主要的模块：连接器和容器
     tomcat处理错误信息，每一个包分配一个属性文件
ext4   

###############################   
part two    tomcat组成    
3（Container）容器 ——》 
{Engine（引擎）,=》 catalina的servlet引擎
Host（主机）,=》 host 表示一个拥有数个上下文的虚拟主机 
Context（上下文）, =》 表示一个web应用，一个context包含一个多个wrapper
 和 Wrapper（包装器）。=》表示一个独立的servlet
 }